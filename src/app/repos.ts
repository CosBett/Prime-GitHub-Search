export class Repos {
  constructor(
    public name: string, public description: string, public html_url: string, public deployments_url: string, public created_at: number, public updated_at: number,
  ) {

  }
}

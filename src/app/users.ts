export class Users {
  constructor(
    public name: string, public avatar_url: string, public login: string, public bio: string, public public_repos: string, public location: string, public followers: number, public following: number,
  ) {

  }
}
